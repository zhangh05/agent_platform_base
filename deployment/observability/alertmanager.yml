route:
  receiver: operator-config-required
  group_by: [alertname, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: operator-config-required

# Production operators must replace the empty receiver with their approved
# email, webhook or incident-management integration before go-live.
