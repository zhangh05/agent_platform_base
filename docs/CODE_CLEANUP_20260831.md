# 历史清理审计（2026-08-31）

本文是历史审计档案，不描述当前 API 或运行时合同。当前实现以源码、测试和其余现行文档为准。

## 当时清理目标

清理已废弃的业务耦合、工具别名和兼容层，使平台回到领域中立的 canonical runtime 与扩展边界。

## 历史结论

- 当时网络设备写权限曾由 Skill 选项、设备/连接和允许工具范围实时核定；该历史设计现已废除，当前网络 Skill 默认可配置其范围内设备。
- 已移除的文件路径、旧工具名和旧数据根仅为审计依据，禁止据此恢复实现。

## 后续维护规则

清理子系统时，必须同时检查源码、测试、文档、模板、部署变量、告警、错误码、重试逻辑和前端入口。若历史档案与当前代码冲突，以当前代码为准，并更新现行文档而不是为档案增加兼容代码。
